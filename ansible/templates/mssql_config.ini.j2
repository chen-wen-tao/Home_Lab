; SQL Server Configuration File
; Generated by Ansible for unattended installation

[OPTIONS]
; Specifies a Setup work flow, like INSTALL, UNINSTALL, or UPGRADE. This is a required parameter.
ACTION="Install"

; Feature selection parameters
FEATURES={{ mssql_features }}

; SQL Server instance configuration
INSTANCENAME="{{ mssql_instance_name }}"

; SQL Server instance directory
INSTANCEDIR="{{ mssql_instance_dir }}"

; SQL Server data directory
INSTALLSQLDATADIR="{{ mssql_data_dir }}"

; SQL Server log directory
SQLUSERDBLOGDIR="{{ mssql_log_dir }}"

; SQL Server backup directory
SQLBACKUPDIR="{{ mssql_backup_dir }}"

; SQL Server tempdb directory
SQLTEMPDBDIR="{{ mssql_temp_db_dir }}"

; Security mode
SECURITYMODE="SQL"

; SQL Server system administrator password
SAPWD="{{ mssql_sa_password }}"

; TCP port
TCPENABLED=1
TCPPORT={{ mssql_port }}

; Accept license terms
IACCEPTSQLSERVERLICENSETERMS="True"

; Enable SQL Server Agent
AGTSVCSTARTUPTYPE="Automatic"

; Enable SQL Server Browser
BROWSERSVCSTARTUPTYPE="Automatic"

; SQL Server Engine service account
SQLSVCACCOUNT="NT AUTHORITY\SYSTEM"
SQLSVCPASSWORD=""

; SQL Server Agent service account
AGTSVCACCOUNT="NT AUTHORITY\SYSTEM"
AGTSVCPASSWORD=""

; SQL Server Browser service account
BROWSERSVCSTARTUPTYPE="Automatic"

; Quiet installation
QUIET="True"
QUIETSIMPLE="True"

